export interface Product {
    createdAt: Date,
    title: string,
    cover_image:string,
    price: number,
    description: string,
    author: string,
    publisher: string,
    view_count: number,
    sold_count: number,
    rating: number,
    is_favorite:boolean,
    id: string,
}